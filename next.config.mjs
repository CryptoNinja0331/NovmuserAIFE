/** @type {import('next').NextConfig} */
const nextConfig = {
  // Need to add the following line when want to create a static website build
  // such as deploy on Hostinger
  output: "export",
};

export default nextConfig;
